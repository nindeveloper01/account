spring:
  application:
    name: account
  datasource:
        driver-class-name: org.postgresql.Driver
        url: jdbc:postgresql://localhost:5432/account
        username: postgres
        password: 1234
  mail:
    host: smtp.gmail.com
    port: 587
    username: nindeveloper01@gmail.com
    password: rszr aqio sgvf dljk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: true
  jackson:
    time-zone: Asia/Phnom_Penh
    date-format: yyyy-MM-dd HH:mm:ss
  h2:
    console:
      enabled: true
      path: /h2-console